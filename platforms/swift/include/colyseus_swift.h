/*
 * Umbrella header for the CColyseus module.
 *
 * The SDK's own umbrella is the module's umbrella: include/colyseus.h ships in
 * the same header root, and the core's build fails when a public header does
 * not reach it, so nothing can go missing here without going missing there.
 */
#ifndef COLYSEUS_SWIFT_H
#define COLYSEUS_SWIFT_H

#include <colyseus.h>

#endif /* COLYSEUS_SWIFT_H */
